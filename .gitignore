# deps
node_modules/

# secrets
.env
.env.local

# build / TS
dist/
*.tsbuildinfo

# Foundry (contracts/)
contracts/out/
contracts/cache/
contracts/broadcast/
# deps via `forge install` (ver contracts/README) — no se commitean
contracts/lib/

# OS / editor
.DS_Store
Thumbs.db
